import { SolicitationTypePipe } from './solicitation-type.pipe';

describe('SolicitationTypePipe', () => {
  it('create an instance', () => {
    const pipe = new SolicitationTypePipe();
    expect(pipe).toBeTruthy();
  });
});
