import { DiagnosisStatusPipe } from './diagnosis-status.pipe';

describe('DiagnosisStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new DiagnosisStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
