import { ProfileTypePipe } from './profile-type.pipe';

describe('ProfileTypePipe', () => {
  it('create an instance', () => {
    const pipe = new ProfileTypePipe();
    expect(pipe).toBeTruthy();
  });
});
