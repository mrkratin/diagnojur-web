import { ProcessTypePipe } from './process-type.pipe';

describe('ProcessTypePipe', () => {
  it('create an instance', () => {
    const pipe = new ProcessTypePipe();
    expect(pipe).toBeTruthy();
  });
});
