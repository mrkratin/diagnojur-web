import { ProcessOrganPipe } from './process-organ.pipe';

describe('ProcessOrganPipe', () => {
  it('create an instance', () => {
    const pipe = new ProcessOrganPipe();
    expect(pipe).toBeTruthy();
  });
});
