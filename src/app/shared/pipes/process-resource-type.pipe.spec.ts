import { ProcessResourceTypePipe } from './process-resource-type.pipe';

describe('ProcessResourceTypePipe', () => {
  it('create an instance', () => {
    const pipe = new ProcessResourceTypePipe();
    expect(pipe).toBeTruthy();
  });
});
