import { SolicitationReplyReasonPipe } from './solicitation-reply-reason.pipe';

describe('SolicitationReplyReasonPipe', () => {
  it('create an instance', () => {
    const pipe = new SolicitationReplyReasonPipe();
    expect(pipe).toBeTruthy();
  });
});
