import { SolicitationTimelinePipe } from './solicitation-timeline.pipe';

describe('SolicitationTimelinePipe', () => {
  it('create an instance', () => {
    const pipe = new SolicitationTimelinePipe();
    expect(pipe).toBeTruthy();
  });
});
