import { JusticeTypePipe } from './justice-type.pipe';

describe('JusticeTypePipe', () => {
  it('create an instance', () => {
    const pipe = new JusticeTypePipe();
    expect(pipe).toBeTruthy();
  });
});
