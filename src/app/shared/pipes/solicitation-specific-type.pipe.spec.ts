import { SolicitationSpecificTypePipe } from './solicitation-specific-type.pipe';

describe('SolicitationSpecificTypePipe', () => {
  it('create an instance', () => {
    const pipe = new SolicitationSpecificTypePipe();
    expect(pipe).toBeTruthy();
  });
});
