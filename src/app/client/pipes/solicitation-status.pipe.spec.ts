import { SolicitationStatusPipe } from './solicitation-status.pipe';

describe('SolicitationStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new SolicitationStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
