import { DocumentDeniedTypePipe } from './document-denied-type.pipe';

describe('DocumentDeniedTypePipe', () => {
  it('create an instance', () => {
    const pipe = new DocumentDeniedTypePipe();
    expect(pipe).toBeTruthy();
  });
});
