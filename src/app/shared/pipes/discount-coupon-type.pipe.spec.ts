import { DiscountCouponTypePipe } from './discount-coupon-type.pipe';

describe('DiscountCouponTypePipe', () => {
  it('create an instance', () => {
    const pipe = new DiscountCouponTypePipe();
    expect(pipe).toBeTruthy();
  });
});
