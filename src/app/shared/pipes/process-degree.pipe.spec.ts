import { ProcessDegreePipe } from './process-degree.pipe';

describe('ProcessDegreePipe', () => {
  it('create an instance', () => {
    const pipe = new ProcessDegreePipe();
    expect(pipe).toBeTruthy();
  });
});
